kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: visionops
nodes:
- role: control-plane

- role: worker
  labels:
    node-type: cpu

- role: worker
  labels:
    node-type: gpu
    gpu-pool: inference

- role: worker
  labels:
    node-type: gpu
    gpu-pool: training

- role: worker
  labels:
    node-type: infra
